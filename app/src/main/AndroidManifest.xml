<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.applicaro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".nounChoose"></activity>
        <activity android:name=".longNoun" />
        <activity android:name=".VerbGame" />
        <activity
            android:name=".ui.login.VerbGame"
            android:label="@string/title_activity_verb_game" />
        <activity android:name=".NounGame" />
        <activity android:name=".ModelGame" />
        <activity android:name=".choose" />
        <activity android:name=".AdjGame" />
        <activity android:name=".adjGame" />
        <activity android:name=".verbInfo" />
        <activity android:name=".verbActivity" />
        <activity
            android:name=".properties"
            android:label="@string/title_activity_properties"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>